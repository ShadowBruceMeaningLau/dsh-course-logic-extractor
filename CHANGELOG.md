# Changelog

## v1.6.3 — 2026-08-19

### 变更
- **学习意图与目的解绑**：「想学 XX」本身即完整学习需求，不要求绑定解决问题/达成目标等目的；目的只是常见伴随物，兴趣/自我提升/无理由想学均捕获；判据收敛为「是否在表达想获得某种知识/技能/理解」

## v1.6.2 — 2026-08-19

### 变更
- **学习需求判定改为意图判据**：以「用户是否在表达需要通过获得知识/技能/理解/信息来解决问题、消除困惑或达成目标」为唯一标准，任何表述形式都算（不必出现「学」字）；常见意图示例明确标注「非穷举、非匹配模板」；补充反例（给资料要加工、无学习意图的任务请求）防过度识别；拿不准倾向视为学习需求

## v1.6.1 — 2026-08-19

### 变更
- **学习需求捕获软化（语义判断）**：不再依赖「学 XX」关键词——目标驱动型（「为了 XX 需要学 XX」，目标即需求，由能力点拆解从目标反推）、能力诉求型、路径咨询型均视为学习需求；拿不准时倾向视为学习需求；资料路径始终优先；USAGE 示例同步

## v1.6.0 — 2026-08-19

### 变更
- **能力点升级为多层树形结构**：独立并列的一级能力点 + 从属的次级能力点（可多层嵌套）；覆盖范围过大的能力点必须向下分解，直到叶子能力点为「可学习、可检验的具体单位」；前置/后置依赖标注在叶子能力点之间（父级依赖由子级推导）；能力点清单留档用 Obsidian 目录树写法；资料覆盖标注到叶子或最近从属层

## v1.5.9 — 2026-08-19

### 新增
- **规划留档（0-规划/ 目录）**：能力点清单确定后写入 `0-规划/能力点清单.md`（含前置/后置依赖，可附能力点依赖图 canvas）；资料清单确定后写入 `0-规划/资料清单.md`（含覆盖能力点/状态列），落地过程中逐项更新状态，随交付保留——自主找资料模式专属
- **能力点前置/后置依赖**：阶段一每个能力点必须标注「前置能力点」（高效学习前应先掌握什么）与「后置能力点」（掌握后能支撑什么），依赖关系随清单呈现给用户确认，从而排出高效学习顺序；铁律 7、USAGE、交付结构图同步

## v1.5.8 — 2026-08-19

### 变更
- **任务澄清提前分流**：按用户首条消息直接判断——给资料路径直接走标准流程、给学习需求直接走自主找资料流程、两者都没给则**告知本 agent 用法**（呈现 USAGE.md 引导），不再等待用户二选一；铁律 7 同步

## v1.5.7 — 2026-08-19

### 变更
- **自主找资料：需求阶段升级为「能力点拆解」**——阶段一产出**需要掌握的具体能力点清单**（从原始需求反推覆盖所需的能力点，逐轮与用户确认），**阶段一不推荐任何资料**；阶段二按已确定的能力点逐项匹配资料（每条标注覆盖的能力点）
- **检索铁律**：上网搜集时**官网/官方文档/高质量网络资源必查且不得遗漏**，候选清单中官网来源显式标注；铁律 7 同步

## v1.5.6 — 2026-08-19

### 变更
- **自主找资料流程升级为迭代确认制**：无资料时先要求用户二选一（给路径 or 给需求），两者皆无则持续等待；学习需求与资料清单两个阶段均以「提问+思考+讨论+上网搜集」反复迭代，以用户明确表态「完全确定」为唯一出口；资料落地按「本地优先 → 网络补充 → 缺失决断（自行下载/忽略，用户不表态则停留）」执行；全部就位或用户选择忽略后进入标准流水线。铁律 7 同步更新

## v1.5.5 — 2026-08-19

### 修复
- **mermaid2canvas.mjs 支持多图**：新增 `--index=N` 参数（此前只提取第一个 mermaid 块）；修复数分模块地图 canvas 节点文本缺失（旧脚本不支持单行格式，已用正式脚本重新生成）；统一学习路线图 canvas 为标准 JSON 格式；新增多块提取单元测试（全套 9/9 绿）

## v1.5.4 — 2026-08-19

### 修复
- **README 全面更新**：OCR 说明从 DeepSeek-OCR 改为 GLM-OCR（`ZHIPU_API_KEY`/`zhipuApiKey` 配置）、补齐全部 8 个脚本表、styles/ 与 test/ 目录结构、三种输入模式特性介绍（此前 README 停留在 v1.1.0 时代，会误导新环境用户配置错误的 Key）

## v1.5.3 — 2026-08-19

### 变更
- **canvas 独立目录**：全部流程图画布集中到 `1-报告/画布/` 子目录（模块地图/学习路线图/论文方法流程），报告内标注「Canvas 版：画布/文件名.canvas」；SKILL/USAGE 结构图同步

## v1.5.2 — 2026-08-19

### 变更
- **风格样本命名重构**：`reference/` 目录更名为 `styles/`（排版风格样本集，非笔记）；文件更名：`高等代数.md → 数学.md`、`工程类笔记.md → 工程.md`、`文科类笔记.md → 文科.md`、`论文笔记.md → 论文.md`；`reference.config.json → styles.config.json`（字段 `defaultReference → defaultStyle`）；SKILL/USAGE 措辞同步（参考笔记 → 风格样本）

## v1.5.1 — 2026-08-19

### 新增
- **流程图全 canvas 化**：报告每出现一个 mermaid 流程图即同步产出 `.canvas`（mermaid 保留 + 标注「Canvas 版」）；新增正式脚本 `scripts/mermaid2canvas.mjs`（自动提取 mermaid 块并转换，支持单行定义/带文本边/纯边三种行格式，按依赖分层布局）+ 单元测试；存量补数分学习路线图 canvas

## v1.5.0 — 2026-08-19

### 新增
- **论文模式（双视角）**：资料形态识别新增「论文型」；研究七要素（问题链/方法动机/证据链/贡献定位/叙事逻辑/衔接信号/评估载体）+ 学习七要素（前置依赖/精读顺序/复现路径/延伸方向）；论文版交付：逻辑链分析（双视角七节）+ 精读路线 + 方法流程 canvas + 「论文」风格参考（`reference/论文笔记.md`）
- **自主找资料模式**：无资料、只有学习需求时触发——需求澄清 → **本地资料库优先检索**（默认探测 `~/Obsidian本地库/书籍管理/资料`，找不到则询问用户）→ 网络检索补充 → 三个确认点（候选清单勾选 / 网络获取动作逐个确认 / 深度档位），全程确认制，未确认不执行；网络资料存入课程 `原始资料/` 子目录
- 铁律第 7 条：确认制（自主找资料）

### 变更
- 风格集新增「论文」档

## v1.4.0 — 2026-08-19

### 新增
- **OCR 引擎切换为 GLM-OCR**（智谱 `glm-ocr`，专用端点 `layout_parsing`）：文本/公式/表格识别 SOTA 级；Key 走 `~/.dsh/free-vision.json` 的 `zhipuApiKey` 或 `ZHIPU_API_KEY`；≤100 页且 ≤50MB 的 PDF 支持整本直传；图片 ≤10MB
- **模块地图 canvas 必需化**：每门课必产 `1-报告/<课程名>-模块地图.canvas`（Obsidian 原生画布，与报告第 3 节 mermaid 一一对应）
- **更新模式**：教材改版/补页走增量流程（`--pages` 区间重转录 → 重合并 → 只重做受影响分卷 → 指纹对比提示差异）
- **单元测试**（`scripts/test/`，node:test）：combine 合并/公式转换、verify 对账统计、stylevol 排序与 TOC 感知校验；`npm test` 一键运行
- **CI**（GitHub Actions）：push/PR 自动跑测试，tag `v*` 自动创建 Release

### 修复
- **combine.mjs 公式转换 bug**：`String.replace` 的 `'$$'` 替换串是特殊模式（表示单个 `$`），导致行间公式 `\[...\]` 被转成 `$...$` 而非 `$$...$$`——改用函数返回字面 `'$$'`（问题自 v1.0 潜伏，由新增测试暴露）

## v1.3.0 — 2026-08-19

### 新增
- **分卷流程正式化**（`scripts/stylevol/`）：`split.mjs` 按章切分直出稿（自动过滤目录区章标题，`--pattern`/`--max-chars` 可调）、`merge.mjs` 按序合并分卷、`check.mjs` 校验（内容一致性 + 结构 QA + 覆盖核对，目录区页码差异自动忽略）；脚本只负责切分/合并/校验，排版风格仍由模型对照参考产出
- **转录质量对账**（`scripts/verify.mjs`）：缺失页/空页/超短页/公式配对统计，产出 `3-材料/质量报告.md`，异常项自动列入报告「待确认」
- **跨课程联动**：资料盘点时扫描 `课程逻辑交付/*/1-报告/` 的 frontmatter 建立课程清单；本课与他课的先修/后继/平行关系写入报告第 6 节「跨课衔接」，证据等级从 `[结构推断]` 起步
- **多风格参考集**：`reference.config.json` 增加 `styles`（数学/工程/文科），新增 `reference/工程类笔记.md`、`reference/文科类笔记.md`；用户说「用工程风格/文科风格」即切换

### 变更
- 无

## v1.2.0 — 2026-08-19

### 新增
- **报告工程化**：逻辑链分析与学习路线图报告统一带 YAML frontmatter（course / source / source_fingerprint / generated / preset_version / depth / status / tags）；重复分析时按 `source_fingerprint` 对比资料是否变化，未变则提示沿用上次结论
- **交付自检清单**：报告尾部固定六项自检（七节齐全 / 证据等级齐全 / `[推测]` 全入待确认 / 证据索引可指 / mermaid 完整 / 无 TODO），交付前逐项勾选
- **学习路线图**：`<课程名>-学习路线图.md` 成为默认第二交付物（3–5 阶段学习路径 + 验收标准 + 跳步警告 + 加速时机），放 `1-报告/`；用户说「不要路线图」可跳过
- **课程总索引**：交付后自动维护 `课程逻辑交付/README.md` 课程总表（课程名 / 类型 / 深度 / 日期 / 状态 / 报告与优化稿链接）
- **网页课程抓取**：web 工具开启 `fetch`，网页课程/URL 形态可直接抓取目录与章节页
- **超大型课程 workflow 规范**：>30 模块或 >2000 页的课程用 workflow 分阶段编排（盘点+骨架 → 分模块并行提取 → 综合+校验）

### 变更
- **combine.mjs 瘦身**：删除 reflow / 目录处理 / 页码剥离 / `--keep-front` 等死代码，仅保留直出模式（`--raw` 兼容）与公式语法转换

### 修复
- 无（承上版）

## v1.1.0 — 2026-08-18

- 参考风格优化版：内置风格参考（`reference/高等代数.md`）+ `reference.config.json` + `USAGE.md`
- 直出稿归档后自动产出参考风格优化稿（`1-报告/<课程名>_参考风格优化版.md`）
- 交付目录重构：`1-报告 / 2-证据 / 3-材料` 三类子目录

## v1.0.0 — 2026-08-18

- 首版：课程逻辑提取器 agent preset，作为 dsh 插件自动安装到 preset roster
- 六步流水线（盘点 → 骨架提取 → 顺序与依赖 → 设计意图 → 自洽校验 → 交付）
- MuPDF 渲染 + DeepSeek-OCR 转录 + `combine.mjs --raw` 直出归档管线
